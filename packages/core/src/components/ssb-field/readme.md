# ssb-field



<!-- Auto Generated Below -->


## Properties

| Property      | Attribute     | Description                                                                                                                            | Type                                                              | Default     |
| ------------- | ------------- | -------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- | ----------- |
| `aria`        | `aria`        | ARIA attributes (JSON string or object). Prefer this over spreading individual aria-* attributes.                                      | `string \| { 'aria-label': string; 'aria-describedby': string; }` | `undefined` |
| `description` | `description` | Muted helper text rendered below the control. Hidden while `error` is set.                                                             | `string`                                                          | `undefined` |
| `error`       | `error`       | Error text rendered in destructive color below the control. When set, the description is hidden.                                       | `string`                                                          | `undefined` |
| `fieldId`     | `field-id`    | Rendered as `for` on the label element. Note: it only associates controls within the same shadow root, so it is mostly presentational. | `string`                                                          | `undefined` |
| `label`       | `label`       | Label text rendered above the control.                                                                                                 | `string`                                                          | `undefined` |
| `required`    | `required`    | Shows a destructive asterisk next to the label.                                                                                        | `boolean`                                                         | `false`     |


----------------------------------------------


